name: Build ZIP (manual only)

on:
  workflow_dispatch:

jobs:
  build_zip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Disabled auto-zip. Use build-exe-win.yml for EXE."
